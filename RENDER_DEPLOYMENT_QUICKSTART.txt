╔══════════════════════════════════════════════════════════════════════════════╗
║                     RENDER DEPLOYMENT - QUICK START                          ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: Configure Environment Variables in Render Dashboard                 │
└──────────────────────────────────────────────────────────────────────────────┘

Go to: Dashboard → Your Service → Environment

Add these variables:

┌─────────────────────┬──────────────────────────────────────────────────────┐
│ Variable            │ Value                                                │
├─────────────────────┼──────────────────────────────────────────────────────┤
│ DATABASE_URL        │ postgresql://user:pass@host:port/db                  │
│                     │ (Get from Supabase → Connection Pooling)             │
├─────────────────────┼──────────────────────────────────────────────────────┤
│ NODE_ENV            │ production                                           │
├─────────────────────┼──────────────────────────────────────────────────────┤
│ ADMIN_PASSWORD      │ [your-secure-password]                               │
│                     │ (Use password generator - min 16 chars)              │
└─────────────────────┴──────────────────────────────────────────────────────┘

⚠️  CRITICAL: Use Supabase "Connection Pooling" string, NOT direct connection!

Example DATABASE_URL format:
postgresql://postgres.xxxx:[PASSWORD]@aws-0-region.pooler.supabase.com:6543/postgres

If password contains special characters, URL-encode them:
  @ → %40
  # → %23
  : → %3A
  % → %25

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: Verify render.yaml is Deployed                                      │
└──────────────────────────────────────────────────────────────────────────────┘

Ensure your repository has the updated render.yaml:

✓ buildCommand includes: npm run prisma:generate
✓ startCommand is: node server/index.js
✓ healthCheckPath is: /api/health
✓ DATABASE_URL is referenced (not DB_TYPE)

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: Push to Git                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

git add .
git commit -m "Production ready: PostgreSQL-only deployment"
git push origin main

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 4: Monitor Deployment                                                  │
└──────────────────────────────────────────────────────────────────────────────┘

Watch Render logs for:

BUILD PHASE:
✓ Installing dependencies...
✓ Generating Prisma client...
✓ Building client...
✓ Compiled successfully

RUNTIME PHASE:
✓ 🚀 SPIN Server Starting...
✓ 🗄️  Database: PostgreSQL (Supabase)
✓ ✅ Connected on attempt 1
✓ ✅ Database tables initialized successfully
✓ ✅ All routes loaded successfully

⚠️  If you see "DATABASE_URL is not configured" → Check Step 1

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 5: Test Deployment                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

1. Health Check:
   https://your-app.onrender.com/api/health
   
   Expected: {"status":"OK","message":"SPIN API is running"}

2. Create a Unit (replace YOUR_PASSWORD):
   curl -X POST https://your-app.onrender.com/api/units \
     -H "Content-Type: application/json" \
     -H "x-admin-key: YOUR_PASSWORD" \
     -d '{"name":"Cardiology","duration_days":30,"workload":"Medium"}'

3. Verify in Frontend:
   https://your-app.onrender.com
   
   - Login with admin password
   - Should see Cardiology unit

┌──────────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

ERROR: "DATABASE_URL is not configured"
→ Add DATABASE_URL in Render environment variables
→ Redeploy after adding

ERROR: "password authentication failed"
→ Check password in DATABASE_URL is correct
→ Ensure special characters are URL-encoded
→ Verify user exists in Supabase

ERROR: "ENETUNREACH"
→ IPv6 issue (already handled in code)
→ Check Supabase service status
→ Wait 30 seconds and check logs again

ERROR: Build fails
→ Check build logs for specific error
→ Ensure package.json is valid
→ Verify Prisma schema is correct

ERROR: "Route not found"
→ Wait for full server startup (can take 30-60s)
→ Check logs for "All routes loaded successfully"

┌──────────────────────────────────────────────────────────────────────────────┐
│ PRODUCTION VERIFICATION CHECKLIST                                           │
└──────────────────────────────────────────────────────────────────────────────┘

Deployment Complete When:
□ Build completed without errors
□ Server shows "✅ Database connection successful"
□ Health endpoint returns {"status":"OK"}
□ No SQLite references in logs
□ Can create units via API
□ Can create interns via API
□ Frontend loads and functions
□ No runtime crashes in logs

┌──────────────────────────────────────────────────────────────────────────────┐
│ WHAT WAS FIXED                                                               │
└──────────────────────────────────────────────────────────────────────────────┘

✅ Removed SQLite dependency from package.json
✅ Updated render.yaml for PostgreSQL-only deployment
✅ Added DATABASE_URL validation at startup
✅ Enhanced error messages and logging
✅ Verified all error handlers are present
✅ Tested production build locally (passed)
✅ No dev dependencies in production
✅ Correct start script (node, not nodemon)

┌──────────────────────────────────────────────────────────────────────────────┐
│ IMPORTANT NOTES                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

🔒 SECURITY:
   - Never commit .env files
   - Use strong ADMIN_PASSWORD (16+ chars)
   - Rotate credentials periodically

📊 PERFORMANCE:
   - Database connection pool: 20 connections
   - Connection timeout: 10 seconds
   - Statement timeout: 30 seconds

🔄 MONITORING:
   - Check Render logs daily for first week
   - Monitor for "database connection lost" errors
   - Watch for memory usage issues

╔══════════════════════════════════════════════════════════════════════════════╗
║                      DEPLOYMENT IS PRODUCTION READY! 🚀                      ║
╚══════════════════════════════════════════════════════════════════════════════╝

Next: Configure environment variables in Render and deploy!

For detailed information, see:
  - PRODUCTION_DEPLOYMENT.md (complete guide)
  - PRODUCTION_FIXES_SUMMARY.md (changelog)
