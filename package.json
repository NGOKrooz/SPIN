{
  "name": "spin-internship-scheduler",
  "version": "1.0.0",
  "description": "SPIN - Smart Physiotherapy Internship Network for UNTH rotation management",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "npm run install-all && cd client && npm install && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "prebuild": "npm run install-all",
    "build-client": "cd client && npm install && npm run build"
  },
  "keywords": ["hospital", "internship", "rotation", "scheduler", "physiotherapy"],
  "author": "SPIN Development Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
