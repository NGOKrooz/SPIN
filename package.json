{
  "name": "spin-internship-scheduler",
  "version": "1.0.0",
  "description": "SPIN - Student Physiotherapy Internship Network for UNTH rotation management",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["hospital", "internship", "rotation", "scheduler", "physiotherapy"],
  "author": "SPIN Development Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
