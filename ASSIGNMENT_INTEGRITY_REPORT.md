ASSIGNMENT INTEGRITY REPORT

Date: 2026-05-17

Summary
- Purpose: Inventory remaining code paths that still treat `pending` as a lifecycle state or perform "active-like" queries using `status: { $in: ['active','pending'] }`.
- Context: Centralized resolver `resolveCurrentAssignment()` and `workflowState` were introduced to keep `status` = `active` while indicating a pending confirmation via `workflowState: 'pending_confirmation'`.

Findings (locations still referencing legacy `pending` lifecycle or active-like queries):

Files and locations (non-exhaustive):
- server/services/rotationService.js
  - queries using `status: { $in: ['active', 'pending'] }` (multiple places)
- server/routes/interns.js
  - queries and logic using `status: { $in: ['active', 'pending']` and direct `status === 'pending'` checks
- server/services/dynamicAssignmentService.js
  - several occupancy/count queries using `status: { $in: ['active', 'pending'] }`
  - code that logs `ACTIVE -> PENDING` transitions
- server/services/rotationPlanService.js
  - queries using `status: { $in: ['active', 'pending'] }` and `isActiveLikeAssignment` checks
- server/routes/dashboard.js
  - counts using `status: { $in: ['active', 'pending'] }`
- server/routes/units.js
  - unit-level occupancy queries using `status: { $in: ['active', 'pending'] }`
- server/services/internService.js
  - intern status derivation using `status === 'pending'` (updated to check `workflowState` but may still have patterns)
- server/scripts/verify-confirmation-workflow.js
  - test script contains `isActiveLikeStatus` helper and filters expecting `pending` as active-like
- server/__tests__/*.js
  - multiple tests still assert presence of `pending` lifecycle (these tests may need updating to reflect workflowState semantics)

Counts (approx from repository grep):
- Files referencing `status: { $in: ['active', 'pending'] }`: ~10 locations
- Files checking `status === 'pending'`: ~6 locations
- Tests expecting legacy `pending` behavior: several (see `server/__tests__`)

Implications
- Runtime: Code that counts active assignments, computes unit occupancy, or selects the current rotation may double-count or incorrectly resolve when some code still treats `pending` as lifecycle-active while other code uses `resolveCurrentAssignment()`.
- Tests: Some tests failed in `npm test` due to changes; adjust tests to verify `workflowState` semantics instead of `status === 'pending'` where appropriate.

Recommendations (next actions)
1. Replace all queries of the form `status: { $in: ['active','pending'] }` with `status: 'active'` where the intention is to count active lifecycle assignments; use `workflowState: 'pending_confirmation'` to detect awaiting-confirmation flows.
2. Use `resolveCurrentAssignment(subject)` (from `assignmentUtils`) for canonical current-assignment selection everywhere instead of ad-hoc filters. Prefer to pass populated rotation arrays where possible.
3. Update dynamic assignment and occupancy functions to use normalized rotation objects (call `normalizeRotation` or `resolveCurrentAssignment`) before counting.
4. Update tests in `server/__tests__` to assert `workflowState` or to use the resolver; remove expectations that `pending` is a lifecycle status unless specifically testing legacy behavior.
5. Run the full test suite after each batch of replacements and update tests where semantics changed.

Suggested migration steps (safe rollout)
- Phase A: Code-only replacements in non-critical paths (routes that are admin-only). Run tests and smoke-test locally.
- Phase B: Replace in dynamic services (`dynamicAssignmentService`, `rotationPlanService`) and run staging load tests.
- Phase C: Update tests and integration scripts (`verify-confirmation-workflow.js`).
- Phase D: Remove legacy `pending` enumeration from `Rotation` model once all usage is migrated and tests pass; keep database migration notes if old rotations exist with `status='pending'`.

Notes
- I can automatically patch the remaining files to use `resolveCurrentAssignment()` and `workflowState` semantics. Would you like me to: (A) create a PR-style patch for all occurrences, or (B) update them incrementally (file-by-file) so you can review each change? 

Generated by GitHub Copilot assistant as part of assignment resolver rebuild work.
